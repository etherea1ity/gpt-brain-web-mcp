class ExtensionBridge:
    """Reserved adapter for a bundled Playwright Chromium extension.

    V1 keeps this as a stub so result/source extraction can later move to a
    controlled extension loaded only in the dedicated Playwright profile.
    """
    enabled = False
